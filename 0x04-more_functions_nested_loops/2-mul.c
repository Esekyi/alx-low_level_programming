#include "main.h"

/**
 * mul - check main.
 * @a: an input number as parameter
 * @b: an input number as parameter
 * Description: function to multiply two integers.
 * Return: int value result.
 */

int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
